{
  "name": "nuxt-meteor",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "DEBUG=nuxt:* nuxt dev --spa",
    "build": "nuxt build --spa",
    "prestart": "npm run build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "test": "echo \"Error: no test specified\" && exit 1",
    "bundler": "meteor-client bundle — config=meteor-client.config.json"
  },
  "license": "MIT",
  "dependencies": {
    "nuxt-edge": "2.0.0-25523369.7c85202"
  },
  "config": {
    "nuxt": {
      "host": "0.0.0.0",
      "port": "4100"
    }
  }
}
